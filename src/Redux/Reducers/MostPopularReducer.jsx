import {SHOW_MOST_POPULAR_MOVIES} from '../Actions/MostPopularAction';

const initialState = {
    mostPopular: []
}

const mostPopularReducer = (state = initialState, action) => {
    switch(action)
}